# secrets / local state
.env
*.db
*.db-journal
client/routes.json
client/ks-sync.log

# 交接用的匯出檔 (ksdb.tar.gz 內含 webhook URL, image tar 很大) — 絕不進版控
/dist/
*.tar.gz

# python
__pycache__/
*.pyc
.venv/
*.egg-info/
build/

# 舊的扁平版殘留 (已改成 bot/ + client/)
/ks.db
